{
  "installCommand": "git clone https://github.com/flutter/flutter.git --depth 1 -b stable $HOME/flutter && export PATH=\"$HOME/flutter/bin:$PATH\" && flutter precache --web",
  "buildCommand": "export PATH=\"$HOME/flutter/bin:$PATH\" && flutter build web --release",
  "outputDirectory": "build/web"
}
