# SSH 授权公钥交付计划

## 背景
用户提供了目标仓库地址，并要求先提供 public key 用于授权后续 push。

## 目标
1. 生成专用 SSH 密钥对（若不存在则创建）。
2. 返回 public key 给用户用于 GitHub 授权。
3. 授权后执行远程配置、push、PR 流程。

## 步骤
1. 生成 `~/.ssh` 下的专用 ed25519 key。
2. 输出 `.pub` 内容给用户。
3. 等待用户完成 GitHub key 授权并确认。
