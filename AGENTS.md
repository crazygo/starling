# Agent Memory

## Planning + PR rule
For each feature or task:
1. Create a markdown plan file in `docs/plans/`.
2. Name the file with a `YYYY-mm-dd-hh-mm` prefix.
3. After finishing coding and creating a PR, use the plan content as the PR message so reviewers can see the background and reasoning.
