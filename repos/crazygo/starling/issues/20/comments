@copilot 请帮忙确认一个可疑的问题：\n\nPR 里重新生成的 `culture_western.bin` 大小为 **10.8 KB**，但原来 main 分支上的是 **79.8 KB**，缩小了约 7 倍。\n\n请检查：\n1. 重新生成的 `culture_western.bin` 里，88 个星座的 `boundary` 字段是否真的包含了多边形顶点数据（非空数组）？\n2. 根据你上一条评论，CDS 在沙箱中被防火墙屏蔽，所有 3 个镜像都失败了，因此 boundary 数据实际上是空的。\n3. 请在仓库的 [Copilot coding agent settings](https://github.com/crazygo/starling/settings/copilot/coding_agent) 里将以下域名加入 allowlist 后重新生成：\n   - `cdsarc.cds.unistra.fr`\n   - `vizier.cds.unistra.fr`\n\n或者，如果无法配置 allowlist，请直接将 `bound_20.dat` 的内容通过其他可访问的镜像下载后写入 `sources/iau/constellation_boundaries.csv`，再重新跑 pipeline，确保 `culture_western.bin` 包含完整的 boundary 数据（文件大小应接近 79.8 KB）。